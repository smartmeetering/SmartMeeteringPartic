import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SmartMeeteringApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
